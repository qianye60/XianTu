/**
 * @fileoverview 任务系统规范
 *
 * 【职责】
 * - 定义任务系统的开关控制
 * - 规范任务生成的触发条件
 */

export const QUEST_SYSTEM_RULES = `
# 规则：任务系统

## 系统开关（最高优先级）
生成任务前必须检查：\`SaveData.社交.任务.配置.启用系统任务 === true\`

## 触发条件（必须同时满足）
1. **系统开关已启用**
2. **玩家明确意图**：输入包含"任务"/"任务榜"/"接任务"等关键词
3. **任务列表未满**：当前任务数量 < \`SaveData.社交.任务.配置.默认任务数量\`

## 系统关闭时严禁
- 生成任何任务对象
- 在text中提及"任务榜"/"接取任务"/"任务奖励"
- 使用 \`push\` 到 \`社交.任务.当前任务列表\`

## 任务生成规范
- **数量**：遵循 \`配置.默认任务数量\`
- **类型**：遵循 \`配置.系统任务类型\`
- **质量**：符合当前境界、场景、剧情
- **频率**：禁止每回合自动生成，禁止仅因"到达任务堂"就生成
`.trim()
