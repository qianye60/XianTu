# 大道朝天 - 多语言切换功能方案

## 📋 方案概述

本方案为《大道朝天》游戏设计一套完整的国际化(i18n)解决方案,支持简体中文、繁体中文、英文等多种语言,为全球玩家提供本地化体验。

---

## 🎯 核心目标

1. **无缝切换** - 运行时动态切换语言,无需刷新页面
2. **持久化存储** - 用户语言偏好保存至localStorage和存档
3. **完整覆盖** - UI界面、游戏内容、系统提示全部支持多语言
4. **易于维护** - 结构化的翻译文件,便于后续添加新语言
5. **性能优化** - 按需加载语言包,避免首屏加载过慢

---

## 🛠️ 技术方案

### 1. 国际化库选择

**推荐使用: Vue I18n (vue-i18n@9.x)**

**理由:**
- Vue官方推荐的国际化解决方案
- 完美支持Vue 3 Composition API
- 轻量级(~20KB gzipped)
- 支持TypeScript类型提示
- 支持复数、日期、数字格式化
- 支持动态语言切换

**安装命令:**
```bash
npm install vue-i18n@9
```

---

## 📁 项目结构

```
src/
├── locales/                    # 语言包目录
│   ├── index.ts               # i18n配置入口
│   ├── zh-CN.ts               # 简体中文
│   ├── zh-TW.ts               # 繁体中文
│   ├── en.ts                  # 英文
│   └── modules/               # 分模块翻译
│       ├── common.ts          # 通用文本
│       ├── character.ts       # 角色相关
│       ├── game.ts            # 游戏玩法
│       ├── ui.ts              # UI界面
│       └── settings.ts        # 设置面板
├── composables/
│   └── useI18n.ts             # i18n组合式函数封装
└── types/
    └── i18n.d.ts              # 类型定义
```

---

## 🔧 实现步骤

### Step 1: 安装依赖

```bash
npm install vue-i18n@9
```

### Step 2: 创建语言包结构

#### `src/locales/modules/common.ts`
```typescript
export default {
  'zh-CN': {
    app: {
      title: '大道朝天',
      loading: '加载中...',
      save: '保存',
      cancel: '取消',
      confirm: '确认',
      success: '成功',
      error: '错误',
      warning: '警告',
      info: '提示'
    },
    nav: {
      home: '首页',
      game: '游戏',
      character: '角色',
      settings: '设置',
      about: '关于'
    }
  },
  'zh-TW': {
    app: {
      title: '大道朝天',
      loading: '載入中...',
      save: '儲存',
      cancel: '取消',
      confirm: '確認',
      success: '成功',
      error: '錯誤',
      warning: '警告',
      info: '提示'
    },
    nav: {
      home: '首頁',
      game: '遊戲',
      character: '角色',
      settings: '設定',
      about: '關於'
    }
  },
  'en': {
    app: {
      title: 'SkywardPath',
      loading: 'Loading...',
      save: 'Save',
      cancel: 'Cancel',
      confirm: 'Confirm',
      success: 'Success',
      error: 'Error',
      warning: 'Warning',
      info: 'Info'
    },
    nav: {
      home: 'Home',
      game: 'Game',
      character: 'Character',
      settings: 'Settings',
      about: 'About'
    }
  }
};
```

#### `src/locales/modules/settings.ts`
```typescript
export default {
  'zh-CN': {
    settings: {
      title: '游戏设置',
      subtitle: '自定义您的游戏体验',
      display: '显示设置',
      game: '游戏设置',
      advanced: '高级设置',
      language: '语言',
      theme: '主题模式',
      uiScale: '界面缩放',
      fontSize: '文字大小',
      debugMode: '调试模式',
      reset: '重置',
      export: '导出',
      import: '导入'
    }
  },
  'zh-TW': {
    settings: {
      title: '遊戲設定',
      subtitle: '自訂您的遊戲體驗',
      display: '顯示設定',
      game: '遊戲設定',
      advanced: '進階設定',
      language: '語言',
      theme: '主題模式',
      uiScale: '介面縮放',
      fontSize: '文字大小',
      debugMode: '偵錯模式',
      reset: '重設',
      export: '匯出',
      import: '匯入'
    }
  },
  'en': {
    settings: {
      title: 'Game Settings',
      subtitle: 'Customize Your Experience',
      display: 'Display Settings',
      game: 'Game Settings',
      advanced: 'Advanced Settings',
      language: 'Language',
      theme: 'Theme',
      uiScale: 'UI Scale',
      fontSize: 'Font Size',
      debugMode: 'Debug Mode',
      reset: 'Reset',
      export: 'Export',
      import: 'Import'
    }
  }
};
```

### Step 3: 创建i18n配置文件

#### `src/locales/index.ts`
```typescript
import { createI18n } from 'vue-i18n';
import common from './modules/common';
import settings from './modules/settings';
// 导入其他模块...

// 合并所有翻译
function mergeMessages() {
  const locales = ['zh-CN', 'zh-TW', 'en'];
  const messages: Record<string, any> = {};
  
  locales.forEach(locale => {
    messages[locale] = {
      ...common[locale],
      ...settings[locale],
      // 合并其他模块...
    };
  });
  
  return messages;
}

// 从localStorage或浏览器获取默认语言
function getDefaultLocale(): string {
  const saved = localStorage.getItem('dad_game_locale');
  if (saved) return saved;
  
  const browserLang = navigator.language;
  if (browserLang.startsWith('zh')) {
    return browserLang.includes('TW') || browserLang.includes('HK') ? 'zh-TW' : 'zh-CN';
  }
  return 'en';
}

const i18n = createI18n({
  legacy: false, // 使用Composition API模式
  locale: getDefaultLocale(),
  fallbackLocale: 'zh-CN', // 回退语言
  messages: mergeMessages(),
  globalInjection: true, // 全局注入$t
});

export default i18n;
```

### Step 4: 在main.ts中注册i18n

#### `src/main.ts` (修改)
```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'
import i18n from './locales' // 导入i18n
import 'ol/ol.css'
import './style.css'
import './styles/panel-theme.css'
import './styles/theme-overrides.css'
import './utils/consolePatch'
import { migrateData } from './utils/indexedDBManager'

async function initializeApp() {
  console.log('【应用启动】开始初始化流程...');
  
  await migrateData();
  
  console.log('【应用启动】数据迁移检查完成，开始挂载Vue应用');
  const app = createApp(App);
  app.use(createPinia());
  app.use(router);
  app.use(i18n); // 注册i18n
  app.mount('#app');
  
  console.log('【应用启动】✅ Vue应用已成功挂载');
}

initializeApp();
```

### Step 5: 创建语言切换Composable

#### `src/composables/useI18n.ts`
```typescript
import { computed } from 'vue';
import { useI18n as useVueI18n } from 'vue-i18n';

export function useI18n() {
  const { t, locale, availableLocales } = useVueI18n();

  // 当前语言名称
  const currentLocaleName = computed(() => {
    const names: Record<string, string> = {
      'zh-CN': '简体中文',
      'zh-TW': '繁體中文',
      'en': 'English'
    };
    return names[locale.value] || locale.value;
  });

  // 切换语言
  const changeLocale = async (newLocale: string) => {
    if (!availableLocales.includes(newLocale)) {
      console.warn(`Language ${newLocale} is not available`);
      return;
    }

    locale.value = newLocale;
    localStorage.setItem('dad_game_locale', newLocale);
    
    // 更新HTML lang属性
    document.documentElement.lang = newLocale;

    console.log(`[i18n] Language changed to: ${newLocale}`);
  };

  return {
    t,
    locale,
    currentLocaleName,
    availableLocales,
    changeLocale
  };
}
```

### Step 6: 在SettingsPanel中添加语言切换

#### `src/components/dashboard/SettingsPanel.vue` (添加语言设置项)
```vue
<template>
  <div class="settings-panel">
    <!-- 现有内容保持不变 -->
    
    <!-- 在"显示设置"区块添加语言选项 -->
    <div class="settings-section">
      <div class="section-header">
        <h4 class="section-title">🎨 {{ t('settings.display') }}</h4>
      </div>
      <div class="settings-list">
        <!-- 新增：语言选择 -->
        <div class="setting-item">
          <div class="setting-info">
            <label class="setting-name">{{ t('settings.language') }}</label>
            <span class="setting-desc">{{ t('settings.languageDesc', '选择界面显示语言') }}</span>
          </div>
          <div class="setting-control">
            <select v-model="currentLocale" class="setting-select" @change="onLanguageChange">
              <option value="zh-CN">简体中文</option>
              <option value="zh-TW">繁體中文</option>
              <option value="en">English</option>
            </select>
          </div>
        </div>
        
        <!-- 现有的其他设置项 -->
        <div class="setting-item">
          <div class="setting-info">
            <label class="setting-name">{{ t('settings.theme') }}</label>
            <!-- ... -->
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { useI18n } from '@/composables/useI18n';

const { t, locale, changeLocale } = useI18n();
const currentLocale = ref(locale.value);

const onLanguageChange = async () => {
  await changeLocale(currentLocale.value);
  // 可选：显示切换成功提示
  toast.success(t('settings.languageChanged', '语言已切换'));
};

onMounted(() => {
  currentLocale.value = locale.value;
});
</script>
```

### Step 7: 在UIStore中添加语言状态

#### `src/stores/uiStore.ts` (添加语言状态)
```typescript
import { defineStore } from 'pinia';
import { ref } from 'vue';

export const useUIStore = defineStore('ui', () => {
  // 现有状态...
  
  // 语言设置
  const currentLocale = ref<string>(localStorage.getItem('dad_game_locale') || 'zh-CN');
  
  function setLocale(locale: string) {
    currentLocale.value = locale;
    localStorage.setItem('dad_game_locale', locale);
    document.documentElement.lang = locale;
  }
  
  return {
    // 现有返回值...
    currentLocale,
    setLocale,
  };
});
```

---

## 🌍 语言包管理策略

### 模块化翻译文件

建议按功能模块拆分翻译文件:

```
src/locales/modules/
├── common.ts          # 通用文本(按钮、标签等)
├── character.ts       # 角色创建、属性
├── game.ts            # 游戏玩法、任务
├── ui.ts              # UI组件
├── settings.ts        # 设置面板
├── quest.ts           # 任务系统
├── inventory.ts       # 背包物品
└── errors.ts          # 错误提示
```

### 翻译文本命名规范

```typescript
{
  // ✅ 推荐：模块.功能.具体文本
  'character.creation.title': '创建角色',
  'character.attributes.rootBone': '根骨',
  
  // ❌ 不推荐：过于扁平
  'createCharacter': '创建角色',
  'rootBone': '根骨',
}
```

---

## 🎨 使用方法

### 在Vue组件中使用

#### 模板中使用
```vue
<template>
  <div>
    <h1>{{ t('app.title') }}</h1>
    <button>{{ t('app.save') }}</button>
    <p>{{ t('settings.subtitle') }}</p>
  </div>
</template>

<script setup lang="ts">
import { useI18n } from 'vue-i18n';
const { t } = useI18n();
</script>
```

#### 脚本中使用
```typescript
import { useI18n } from 'vue-i18n';

const { t } = useI18n();

// 在函数中使用
const showMessage = () => {
  toast.success(t('app.success'));
};
```

### 在非Vue文件中使用

#### `src/utils/toast.ts` (修改)
```typescript
import i18n from '@/locales';

const { t } = i18n.global;

export const toast = {
  success: (message: string) => {
    // 支持直接传入翻译key
    const text = message.includes('.') ? t(message) : message;
    // 显示提示...
  },
  error: (message: string) => {
    const text = message.includes('.') ? t(message) : message;
    // 显示错误...
  }
};
```

---

## 📊 数据持久化

### 语言偏好保存策略

1. **localStorage** - 全局语言偏好
   ```typescript
   localStorage.setItem('dad_game_locale', 'zh-CN');
   ```

2. **存档系统** - 角色级别的语言设置
   ```typescript
   // 在gameStateStore中添加
   systemConfig: {
     locale: 'zh-CN', // 该存档使用的语言
     // 其他配置...
   }
   ```

3. **同步机制**
   - 优先使用存档中的语言设置
   - 存档未指定则使用localStorage
   - 都未设置则使用浏览器语言

---

## ⚡ 性能优化

### 1. 按需加载(可选)

对于大型翻译文件,可实现动态导入:

```typescript
// src/locales/index.ts
import { createI18n } from 'vue-i18n';

const i18n = createI18n({
  legacy: false,
  locale: 'zh-CN',
  messages: {
    'zh-CN': {} // 初始为空
  }
});

// 动态加载语言包
export async function loadLocaleMessages(locale: string) {
  const messages = await import(`./modules/${locale}.ts`);
  i18n.global.setLocaleMessage(locale, messages.default);
}
```

### 2. 翻译缓存

Vue I18n自带缓存机制,无需额外处理。

---

## 🧪 测试建议

### 1. 单元测试

```typescript
import { describe, it, expect } from 'vitest';
import { useI18n } from '@/composables/useI18n';

describe('i18n', () => {
  it('should translate correctly', () => {
    const { t } = useI18n();
    expect(t('app.title')).toBe('大道朝天');
  });

  it('should switch language', async () => {
    const { changeLocale, locale } = useI18n();
    await changeLocale('en');
    expect(locale.value).toBe('en');
  });
});
```

### 2. 完整性测试

创建脚本检查所有语言包是否包含相同的key:

```typescript
// scripts/check-i18n.ts
import zhCN from '../src/locales/zh-CN';
import zhTW from '../src/locales/zh-TW';
import en from '../src/locales/en';

function checkKeys(obj1: any, obj2: any, path = '') {
  // 递归检查所有key是否匹配
  // ...
}

checkKeys(zhCN, zhTW);
checkKeys(zhCN, en);
```

---

## 📝 迁移步骤

### 阶段1: 基础设施 (第1-2天)
1. 安装vue-i18n依赖
2. 创建locales目录结构
3. 配置i18n并集成到main.ts
4. 创建useI18n composable

### 阶段2: 核心翻译 (第3-5天)
5. 提取现有中文文本到zh-CN.ts
6. 翻译繁体中文版本(可使用工具辅助)
7. 翻译英文版本
8. 在设置面板添加语言切换选项

### 阶段3: 全面替换 (第6-8天)
9. 逐个组件替换硬编码文本为$t()
10. 更新toast/debug等工具函数
11. 测试所有页面的语言切换

### 阶段4: 优化完善 (第9-10天)
12. 添加语言切换动画效果
13. 优化加载性能
14. 编写文档和使用指南
15. 全面测试和修复问题

---

## 🔮 未来扩展

### 1. 更多语言支持
- 日语 (ja)
- 韩语 (ko)
- 德语 (de)
- 法语 (fr)

### 2. AI辅助翻译
集成翻译API自动生成初版翻译:
```typescript
async function translateWithAI(text: string, targetLang: string) {
  // 调用翻译API
}
```

### 3. 用户贡献翻译
建立社区翻译平台,让玩家参与翻译工作。

---

## ⚠️ 注意事项

1. **修仙术语翻译**
   - 建议保留核心修仙术语的音译(如"筑基"→"Zhuji")
   - 重要概念提供注释说明

2. **文本长度适配**
   - 不同语言文本长度差异大(中文紧凑,英文较长)
   - UI设计需预留足够空间
   - 使用CSS `text-overflow: ellipsis` 处理溢出

3. **数字和日期格式**
   ```typescript
   // 使用vue-i18n的格式化功能
   {{ n(1000, 'currency') }}  // ¥1,000
   {{ d(new Date(), 'long') }} // 2025年10月29日
   ```

4. **RTL语言支持**(如阿拉伯语)
   - 当前暂不支持
   - 未来如需支持需额外处理CSS `direction: rtl`

---

## 📚 参考资源

- [Vue I18n官方文档](https://vue-i18n.intlify.dev/)
- [MDN - Intl API](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Intl)
- [Google翻译API](https://cloud.google.com/translate)

---

## 🎯 总结

本方案提供了一套完整、可扩展的多语言解决方案,适合《大道朝天》项目的技术栈和业务需求。通过模块化的翻译文件管理、清晰的命名规范、以及完善的持久化策略,可以高效实现国际化功能,为游戏的全球化奠定基础。

**预估开发周期:** 10个工作日
**风险等级:** 低
**维护成本:** 中等

---

*文档版本: v1.0*  
*创建日期: 2025-10-29*  
*作者: 千夜*